# GymVision AI - Backend development and test dependencies
#
#   pip install -r requirements-dev.txt
#
# Includes everything in requirements.txt. Test tooling is kept out of the
# runtime file so production deployments never install it.

-r requirements.txt

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
httpcore==1.0.9
certifi==2026.7.22
iniconfig==2.3.0
pluggy==1.6.0
packaging==26.2
Pygments==2.20.0
colorama==0.4.6

# The API tests run against SQLite because PostgreSQL is not always available.
# Production always uses PostgreSQL via asyncpg.
aiosqlite==0.22.1

requests